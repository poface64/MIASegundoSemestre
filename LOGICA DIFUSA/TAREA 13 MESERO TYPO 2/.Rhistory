import pyit2fls as fzl2
import numpy as np
domain = linspace(-1.5, 1.5, 100)
domain = np.linspace(-1.5, 1.5, 100)
set1 = T1FS(domain, trapezoid_mf, [-1.25, -0.75, -0.25, 0.25, 1.])
import pyit2fls
import numpy as np
# Cargar un ejemplo sencillo
domain = np.linspace(-1.5, 1.5, 100)
set1 = T1FS(domain, trapezoid_mf, [-1.25, -0.75, -0.25, 0.25, 1.])
import pyit2fls
import numpy as np
# Cargar un ejemplo sencillo
domain = np.linspace(-1.5, 1.5, 100)
set1 = T1FS(domain, trapezoid_mf, [-1.25, -0.75, -0.25, 0.25, 1.])
import pyit2fls as fz2
import numpy as np
set1 = fz2.T1FS(domain, trapezoid_mf, [-1.25, -0.75, -0.25, 0.25, 1.])
set1 = fz2.T1FS(domain, fz2.trapezoid_mf, [-1.25, -0.75, -0.25, 0.25, 1.])
set1 = fz2.T1FS(domain, fz2.trapezoid_mf, [-1.25, -0.75, -0.25, 0.25, 1.])
domain = np.linspace(-1.5, 1.5, 100)
set1 = fz2.T1FS(domain, fz2.trapezoid_mf, [-1.25, -0.75, -0.25, 0.25, 1.])
set2 = fz2.T1FS(domain, fz2.trapezoid_mf, [-0.25, 0.25, 0.75, 1.25, 1.])
T1FS_plot(set1, set2, legends=["Trapezoidal Set 1", "Trapezoidal Set 2", ])
fz2.T1FS_plot(set1, set2, legends=["Trapezoidal Set 1", "Trapezoidal Set 2", ])
import pyit2fls as fz2
import numpy as np
# Cargar un ejemplo sencillo
domain = np.linspace(0, 4, 1001)
